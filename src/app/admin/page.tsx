export default function AdminOverviewPage() {
  return (
    <div>
      <h1 className="font-display text-2xl font-semibold text-ink-800">Admin overview</h1>
      <p className="mt-2 text-ink-500">
        Manage feature toggles, categories, quizzes, blog, site pages, and users from the sections
        on the left.
      </p>
    </div>
  );
}
